import { createDesktopShellPlan } from "./shell-plan";

console.log(JSON.stringify(createDesktopShellPlan(), null, 2));
console.log("Electron/Vite/Vue runtime is intentionally scaffolded; wire real Electron startup after V1 shell tests land.");
