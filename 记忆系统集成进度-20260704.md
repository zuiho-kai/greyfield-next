# 记忆系统集成进度报告

> 更新时间: 2026-07-04  
> 项目: Greyfield Next - Memory System Refactor

## 已完成的工作

### 1. 创建向后兼容的存根文件 ✅

为了保持现有代码可编译，创建了以下存根文件：

**core-runtime:**
- `memory-atoms.ts` - 旧 Memory Atom 系统的类型定义和空实现
- `memory-context.ts` - 旧 Summary Segment 系统的类型定义和空实现
- `memory-erasure.ts` - 旧记忆删除系统的类型定义和空实现
- `proactive-memory.ts` - 旧主动记忆系统的存根

**persistence:**
- `jsonl-memory-atom-store.ts` - 空实现
- `jsonl-summary-segment-store.ts` - 空实现
- `jsonl-deleted-memory-evidence-store.ts` - 空实现
- `memory-store.ts` - 空实现

### 2. 更新导出文件 ✅

**packages/persistence/src/index.ts:**
```typescript
// 同时导出旧系统（向后兼容）和新系统
export * from "./jsonl-summary-segment-store";
export * from "./jsonl-memory-atom-store";
export * from "./jsonl-deleted-memory-evidence-store";
export * from "./memory-store";
export * from "./memory"; // 新系统
```

**packages/core-runtime/src/index.ts:**
```typescript
// 同时导出旧系统（向后兼容）和新系统
export * from "./memory-atoms";
export * from "./memory-context";
export * from "./memory-erasure";
export * from "./proactive-memory";
export * from "./memory"; // 新系统
```

### 3. 创建集成指南和示例 ✅

- `memory-integration.ts` - 详细的集成指南和示例代码
- `memory-init.ts` - 初始化脚本示例
- `memory/__tests__/integration.test.ts` - 基础集成测试

## 当前状态

### 编译状态 ⚠️
- **主代码**: 可以编译（通过存根文件保持兼容）
- **测试文件**: 有类型错误（约 40+ 处），主要是测试代码使用了旧 API

### 测试错误分类
1. **Mock 对象签名不匹配** - 测试中的 Mock 返回值与新接口不符
2. **已删除的属性访问** - 测试访问了旧类型中存在但新类型中不存在的字段（如 `kind`, `threadId`, `type`）
3. **枚举值变更** - 如 `"hybrid"` 和 `"llm"` 不再是有效的 `MemoryAtomExtractionMode`

## 未完成的工作

### P0 - 必须完成（阻塞功能）

#### 1. 修复测试文件 ❌
需要更新或注释掉以下测试：
- `packages/core-runtime/src/__tests__/runtime-loop.test.ts`
- `packages/core-runtime/src/__tests__/prompt-assembler.test.ts`

**两个选择:**
- **选项 A**: 暂时注释掉旧测试，标记 `// TODO: migrate to new memory system`
- **选项 B**: 完全重写测试以使用新 memory API

**建议**: 选项 A（快速解决，保留历史）

#### 2. 实际集成到 runtime-loop ❌
需要修改 `packages/core-runtime/src/runtime-loop.ts`:

```typescript
// 1. 添加到 interface
export interface GreyfieldRuntimeOptions {
  // ... existing fields
  topicIndexStore?: JsonlTopicIndexStore;
  coreMemoryStore?: SqliteCoreMemoryStore;
}

// 2. 添加到 class
export class GreyfieldRuntime {
  private memoryManager?: MemoryManager;
  
  constructor(options: GreyfieldRuntimeOptions) {
    // ... existing code
    
    if (options.topicIndexStore && options.coreMemoryStore) {
      this.memoryManager = new MemoryManager(
        options.topicIndexStore,
        options.coreMemoryStore,
        options.llm,
        options.sessionStore.sessionId,
        options.persona.id || "default"
      );
    }
  }
  
  // 3. 在 handleTextInput 中集成
  private async handleTextInput(...) {
    // Before assemblePrompt:
    let newMemoryContext = "";
    if (this.memoryManager) {
      const recallResult = await recall(text, this.memoryManager);
      newMemoryContext = formatRecallForPrompt(recallResult);
    }
    
    // After saving response:
    if (this.memoryManager) {
      await this.memoryManager.onNewTurn(userTurn);
      await this.memoryManager.onNewTurn(assistantTurn);
    }
  }
}
```

#### 3. Desktop app 集成 ❌
需要修改 `apps/desktop/src/main/runtime-service.ts`:

```typescript
import { initializeMemoryStores } from "@greyfield/core-runtime";

// 在创建 runtime 时：
const { topicStore, coreStore } = initializeMemoryStores(characterId);

const runtime = new GreyfieldRuntime({
  // ... existing options
  topicIndexStore: topicStore,
  coreMemoryStore: coreStore
});
```

### P1 - 重要（功能完整性）

#### 4. 实现 LLM 话题提取 ❌
当前 `memory-manager.ts` 的 `extractTopics()` 是简单的启发式实现，需要：
- 设计 LLM prompt
- 调用 LLM 批量提取话题
- 解析 JSON 返回

#### 5. 添加基础单元测试 ❌
- 测试 embedding API 调用
- 测试 SQLite 存储基本 CRUD
- 测试召回逻辑

### P2 - 可选（用户体验）

#### 6. 实现 Memory Library UI ❌
复用现有 `SettingsWindow.vue` Memory Library，或后续设计 V2 专属入口:
- 显示核心记忆列表
- 显示话题索引列表
- 删除记忆功能
- 查看来源对话功能

#### 7. 优化和增强 ❌
- 记忆衰减机制（strength 随时间降低）
- 更好的话题提取 prompt
- 性能优化（批量 embedding）

## 技术债务

### 1. 存根文件需要清理
当所有代码迁移到新系统后，需要删除：
- `memory-atoms.ts`
- `memory-context.ts`
- `memory-erasure.ts`
- `proactive-memory.ts`
- 对应的 persistence 存根

### 2. 旧测试需要重写或删除
`runtime-loop.test.ts` 中大量测试基于旧 memory 系统，需要：
- 迁移到新 API
- 或删除并重写

### 3. 依赖问题
项目依赖安装遇到权限问题（Electron 文件被锁定），需要：
- 关闭所有 Electron 进程
- 重新运行 `pnpm install`

## 下一步行动建议

### 立即行动（今天完成）

1. **修复测试文件** - 注释掉旧测试，添加 TODO 标记
   ```bash
   # 在 runtime-loop.test.ts 顶部添加：
   // TODO: These tests use legacy memory system, need migration
   // Most tests are temporarily skipped pending memory system refactor
   ```

2. **集成到 runtime-loop** - 按照上面的代码片段修改

3. **Desktop app 集成** - 修改 runtime-service.ts

### 短期目标（本周）

4. **实现 LLM 话题提取** - 完成核心业务逻辑

5. **基础测试** - 确保新系统可用

### 中期目标（下周）

6. **Memory Library UI** - 用户可见功能

7. **清理技术债务** - 删除存根，重写测试

## 关键文件位置

```
packages/core-runtime/src/
├── memory/                         # 新系统
│   ├── types.ts
│   ├── embedding.ts
│   ├── memory-manager.ts
│   ├── recall.ts
│   └── __tests__/integration.test.ts
├── memory-integration.ts           # 集成指南
├── memory-init.ts                  # 初始化示例
├── memory-atoms.ts                 # 存根（待删除）
├── memory-context.ts               # 存根（待删除）
├── memory-erasure.ts               # 存根（待删除）
└── proactive-memory.ts             # 存根（待删除）

packages/persistence/src/
├── memory/                         # 新系统
│   ├── jsonl-topic-index-store.ts
│   └── sqlite-core-memory-store.ts
├── jsonl-memory-atom-store.ts      # 存根（待删除）
├── jsonl-summary-segment-store.ts  # 存根（待删除）
├── jsonl-deleted-memory-evidence-store.ts  # 存根（待删除）
└── memory-store.ts                 # 存根（待删除）
```

## API Key 信息

```
SiliconFlow Embedding API:
- Base URL: https://api.siliconflow.cn/v1
- API Key: 通过 `SILICONFLOW_API_KEY` 配置，不写入仓库
- 模型: BAAI/bge-m3 或 Qwen/Qwen3-Embedding-0.6B
```

## 风险和注意事项

1. **向后兼容性**: 存根文件保证旧代码可以编译，但不保证运行时行为
2. **测试覆盖率**: 旧测试被跳过后，测试覆盖率会下降
3. **数据迁移**: 没有实现从旧 memory 系统到新系统的数据迁移工具
4. **性能**: 向量检索的性能取决于 SQLite-vss 扩展是否成功加载

## 总结

核心模块已完成，现在处于"可编译但未集成"状态。主要工作是：
1. 修复/跳过旧测试（1小时）
2. 集成到 runtime-loop（2小时）
3. Desktop app 集成（1小时）
4. LLM 话题提取（2小时）

预计还需要 **6-8 小时**完成 P0 和 P1 任务，实现基本可用的新记忆系统。
